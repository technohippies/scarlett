import { Component } from 'solid-js';
import NewTabPage from './NewTabPage'; // Assuming NewTabPage is in the same directory

const App: Component = () => {
  // This component simply renders the main page component.
  // You could add layout wrappers, context providers, etc. here if needed.
  return <NewTabPage />;
};

export default App; 